services:
  localreader-server:
    build: .
    ports:
      - "8997:8000"
    volumes:
      - ./data:/app/data
    environment:
      - ALLOWED_ORIGINS=https://charlesneimog.github.io,http://localhost:8080
    restart: unless-stopped
